apiVersion: v1
kind: Service
metadata:
  name: prediction-api-service
spec:
  selector:
    app: prediction-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
